{
    "header": {
        "message": "Saudações",
        "subcommand": {
            "args": ["...arr"],
            "function": "const now=new Date().getUTCHours()-3;let dayTime;if(now>=20||now<=4){dayTime='Boa noite';}else if(now>=13&&now<=19){dayTime='Boa tarde';}else{dayTime='Bom dia'} return dayTime + (arr ? ' ' + arr.join(', ').split('').reverse().join('').replace(/\\s,/, ' e ').split('').reverse().join('') + '.' : '');"
        }
    },
    "info": {
        "message": "Preciso de informações sobre",
        "subcommand": {
            "args": ["...arr"],
            "function": "return 'Preciso de informações sobre ' + arr.join(', ').split('').reverse().join('').replace(/\\s,/, ' e ').split('').reverse().join('')"
        }
    },
    "moreinfo": {
        "message": "Preciso de mais informações sobre",
        "subcommand": {
            "args": ["...arr"],
            "function": "return 'Preciso de mais informações sobre ' + arr.join(', ').split('').reverse().join('').replace(/\\s,/, ' e ').split('').reverse().join('')"
        }
    },
    "today": {
        "message": "Hoje",
        "subcommand": {
            "function": "const format = (n) => n < 10 ? '0' + n : n; const today = new Date(Date.now()); return `${format(today.getDate())}/${format(today.getMonth()+1)}/${today.getFullYear()}`"
        }
    },
    "now": {
        "message": "Agora",
        "subcommand": {
            "args": ["full = false"],
            "function": "const format = (n) => n < 10 ? '0' + n : n; const now = new Date(Date.now()); return (full ? `${format(now.getDate())}/${format(now.getMonth()+1)}/${now.getFullYear()} `: '') + `${format(now.getHours())}:${format(now.getMinutes())}`"
        }
    },
    "_lastUpdate": "2020-02-03T18:06:27Z"
}